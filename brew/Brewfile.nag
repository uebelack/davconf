# NAG work machines.
#
# The projects under ~/projects/nag are Maven/JVM on Azure infrastructure, so
# select the `java` and `cloud` profiles alongside this one.

brew "unixodbc"    # ODBC driver manager, for the SQL Server clients
