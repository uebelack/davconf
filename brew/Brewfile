# Core — installed on every machine.
#   brew bundle --file=brew/Brewfile

brew "gh"          # GitHub CLI
brew "docker"      # docker CLI (the daemon comes from the cask in Brewfile.dev)
brew "ffmpeg"
brew "qrtool"
brew "serve"

cask "ghostty"
cask "raycast"
cask "maccy"
cask "fork"
cask "claude-code"
cask "font-fira-code-nerd-font"   # the prompt needs a Nerd Font for its glyphs
